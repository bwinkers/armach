"use strict";

var Promise = require('bluebird') // Promise library
;

/**
 * Support uploading a JSON file object with two elements.
 * {
 *
 *     "data": {},
 *     "schema": {}
 * }
 * @param request
 * @returns {string}
 */
exports.root = function (request) {

    return new Promise(function (resolve, reject) {
            resolve('Howdy Ho!');
        });

}



