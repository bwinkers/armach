"use strict";

/**
 * A middleware providing ActiveRules Routing
 *
 * ActiveRules Routing turns JSON schema defined routes into Mach.js ednpoints.
 */
function middleware(app, settings) {

    return function (conn) {

        return conn.call(app);



    }; // End the main return function
}

module.exports = middleware;
