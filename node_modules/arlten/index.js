"use strict";

var Promise = require('bluebird') // Promises
    , _ = require('lodash');

/**
 * ActiveRules Localization helper to be included in connection
 */
var lten = function() {
    // An array of the loaded locales (en-us, es-us, ex-mx, etc)
    // A lcoal code is the key and the value is a localization object
    var locales = [];

    // The default locale
    var defaultLocale = settings.localization.defaultLocale;

}

/**
 * A middleware providing ActiveRules Localization
 */
function middleware(app, settings) {

    return function (conn) {

        conn.lten = lten;

        return conn.call(app);

    }; // End the main return function
}

module.exports = middleware;